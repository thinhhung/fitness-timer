import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  view: {
    backgroundColor: '#1B1811', // Black chocolate
    alignItems: 'center',
    justifyContent: 'center',
    paddingTop: 10,
    paddingBottom: 10,
  },
  text: {
    color: '#fff',
    fontSize: 20,
    textTransform: 'uppercase',
  },
});

export default styles;
